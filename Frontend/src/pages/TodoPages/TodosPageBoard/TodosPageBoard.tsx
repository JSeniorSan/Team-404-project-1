import WorksapceHeader from "../../../entities/Workspace/title/WorksapceHeader";
import TodosBoard from "../../../widgets/todosBoard/TodosBoard";

const TodosPageBoard = () => {
  return (
    <div>
      <WorksapceHeader />
      <TodosBoard />
    </div>
  );
};

export default TodosPageBoard;
