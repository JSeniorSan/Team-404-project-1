import React from "react";
import "./index.scss";
import Wrapper from "../../../shared/ui/wrapper/Wrapper";

const Header = () => {
  return <Wrapper className="header">header</Wrapper>;
};

export default Header;
