Workspace model:
    change: user_id -> creator_id


Task model:
    удалил поле is_completed


Эндпоинты:
    Изменить название таски: был PUT запрос -> стал PATCH
    Изменить название панели: был PUT запрос -> стал PATCH
    Изменить название воркспейса: был PUT запрос -> стал PATCH

    То, что раньше выдавал /kanban/{workspace_id} теперь выдается по
    запросу /workspace/{workspace_id}

Поменялись модели, получаемые по ГЕТ-запросам:
1) Теперь у воркспейса есть вложенное поле panels, содержащее список панелей,
   которые в свою очередь содержат список тасок;
2) И если отдельно делать гет запрос на панель, то у нее, соответственно, будет
   вложенное поле tasks с тасками.

   Подробно можно увидеть в интерактивной документации.